<resources>

    <!-- Base application theme. -->
    <style name="XhsEmoticonsKeyboardTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="XhsEmoticonsKeyboardTheme.NoTitle">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="XhsEmoticonsKeyboardTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="XhsEmoticonsKeyboardTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="emotion_editview">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@null</item>
        <item name="android:layout_gravity">top</item>
        <item name="android:gravity">top</item>
        <item name="android:inputType">textMultiLine</item>
        <item name="android:lines">10</item>
        <item name="android:padding">@dimen/margin_nomal</item>
        <item name="android:textColor">@color/edit_text</item>
        <item name="android:textColorHint">@color/edit_hint_text</item>
        <item name="android:textSize">@dimen/text_nomal</item>
    </style>
</resources>
